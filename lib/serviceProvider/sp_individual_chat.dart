import 'package:flutter/material.dart';

class SPIndividualChat extends StatelessWidget {
  const SPIndividualChat({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('SP Name'),
      ),
      body: const Center(
        child: Text('SPIndividualChat'),
      ),
      
    );
  }
}