import 'package:flutter/material.dart';

class Testsp extends StatelessWidget {
  const Testsp({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Center(
        child: Text("Service Provider login page"),
      ),
    );
  }
}
