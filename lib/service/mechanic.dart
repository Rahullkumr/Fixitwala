import 'package:flutter/material.dart';

class MechanicPage extends StatelessWidget {
  const MechanicPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Mechanic Page")),
    );
  }
}